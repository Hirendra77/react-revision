import React from 'react'

function HelperFunction() {
  return (
    <div>

        {/* Helper function means creating all functions that we need in our code in separate helper file 
        so that we can easily call them when we will be need 
        we can also pass params in helper function*/}
    </div>
  )
}

export default HelperFunction